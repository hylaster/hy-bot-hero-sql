/**
 * The name of bot commands. This is the name the users type in when they wish to use a bot command.
 */
export enum CommandName {
  GetRating = 'getrating',
  GetTop = 'gettop',
  Ping = 'ping',
  Record = 'record',
  Reload = 'reload',
  ResetRank = 'resetrank',
  Roll = 'roll',
  Timer = 'timer'
}
