import { Command } from './command';

const mappings: [number, Command][];

export class CommandLocator {

  private static nameMap = new Map<string, Command>(map)

  static getCommand(name: string) {

  }
}